import Link from 'next/link';
import { activities } from '@/data/activities';

export default function ExperiencesPage() {
  return <main className="min-h-screen bg-stone-50 px-4 py-12"><div className="mx-auto max-w-6xl"><Link href="/" className="text-sm font-bold text-amber-700">← العودة للرئيسية</Link><div className="my-8 max-w-3xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-amber-700">تجارب ثقافية</p><h1 className="mt-2 text-4xl font-black">اصنع جواً تاريخياً يليق بالزائر</h1><p className="mt-4 leading-8 text-slate-600">أفكار بسيطة يمكن لشركات السياحة والفنادق والمرشدين تنفيذها مع المجتمع المحلي، دون المساس بالمواقع أو اختلاق معلومات تاريخية.</p></div><div className="grid gap-6 md:grid-cols-2">{activities.map((activity) => <article key={activity.title} className="rounded-3xl border border-stone-200 bg-white p-6 shadow-soft"><div className="flex justify-between gap-3 text-xs font-bold text-amber-700"><span>{activity.duration}</span><span>{activity.suitableFor}</span></div><h2 className="mt-4 text-2xl font-black">{activity.title}</h2><p className="mt-3 leading-7 text-slate-700">{activity.description}</p><ol className="mt-5 space-y-2 text-sm text-slate-600">{activity.steps.map((step, index) => <li key={step}><span className="ml-2 font-black text-amber-600">{index + 1}.</span>{step}</li>)}</ol></article>)}</div><div className="mt-8 rounded-3xl border border-amber-200 bg-amber-50 p-6 text-sm leading-7 text-amber-950"><strong>مبدأ مهم:</strong> يجب أخذ موافقات الجهات المختصة، احترام خصوصية السكان، منع لمس الآثار، والتأكد من إجراءات السلامة والفتح قبل تنظيم أي فعالية.</div></div></main>;
}
