import Link from 'next/link';
import { itineraries } from '@/data/itineraries';

export default function TripsPage() {
  return (
    <main className="min-h-screen bg-stone-50 px-4 py-12">
      <div className="mx-auto max-w-6xl">
        <Link href="/" className="text-sm font-bold text-amber-700">← العودة للرئيسية</Link>

        <div className="my-8 max-w-3xl">
          <p className="text-sm font-bold uppercase tracking-[0.2em] text-amber-700">رحلات سياحية</p>
          <h1 className="mt-2 text-4xl font-black text-slate-900">خطط رحلاتك الثقافية في العراق</h1>
          <p className="mt-4 leading-8 text-slate-600">
            اختيار رحلات قصيرة ومناسبة للسائح الأجنبي والعربي مع تنسيق بين المواقع الثقافية والطقس وملاءمة المسافات.
          </p>
        </div>

        <div className="grid gap-6 md:grid-cols-2">
          {itineraries.map((trip) => (
            <article key={trip.slug} className="rounded-[2rem] border border-stone-200 bg-white p-6 shadow-soft">
              <div className="text-xs font-bold uppercase tracking-[0.2em] text-amber-700">{trip.duration}</div>
              <h2 className="mt-3 text-3xl font-black text-slate-900">{trip.title}</h2>
              <p className="mt-3 text-sm leading-7 text-slate-700">{trip.summary}</p>

              <div className="mt-5 space-y-3">
                {trip.days.map((day) => (
                  <div key={day.title} className="rounded-2xl border border-stone-200 bg-stone-50 p-4">
                    <div className="text-xs font-bold uppercase tracking-[0.2em] text-amber-700">{day.label}</div>
                    <div className="mt-2 text-lg font-black text-slate-900">{day.title}</div>
                    <ul className="mt-2 space-y-2 text-sm leading-7 text-slate-700">
                      {day.points.map((point) => (
                        <li key={point} className="flex gap-2">
                          <span className="mt-2 h-2.5 w-2.5 shrink-0 rounded-full bg-amber-500" />
                          <span>{point}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                ))}
              </div>
            </article>
          ))}
        </div>
      </div>
    </main>
  );
}
